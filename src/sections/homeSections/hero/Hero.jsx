import React from "react";
import styles from "./Hero.module.scss";

const Hero = () => {
  return (
    <section className={styles.section} id="home">
      <div className={`container ${styles.container}`}>hero</div>
    </section>
  );
};

export default Hero;
