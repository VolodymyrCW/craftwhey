import React from "react";
import styles from "./ProductSlag.module.scss";

const ProductSlag = () => {
  return (
    <section className={styles.section}>
      <div className={`container`}>
        <h2 className={styles.title}>ProductSlag</h2>
      </div>
    </section>
  );
};

export default ProductSlag;
