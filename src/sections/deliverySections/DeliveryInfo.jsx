import React from "react";
import styles from "./DeliveryInfo.module.scss";

const DeliveryInfo = () => {
  return (
    <section className={styles.section}>
      <div className={`container`}>
        <h2 className={styles.title}>DeliveryInfo</h2>
      </div>
    </section>
  );
};

export default DeliveryInfo;
